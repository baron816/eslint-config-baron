# Baron's ESLint Config

## Install

`npm i eslint-config-baron eslint eslint-config-prettier @babel/eslint-parser prettier`

### Add `.eslintrc` file

```
// .eslintrc
{
    "extends": "eslint-config-baron",
    "parser": "@babel/eslint-parser"
}
```
